---
import { Image } from "astro:assets";
import exterior from "../assets/images/exterior.jpg";
import kitchenDining from "../assets/images/kitchen-dining.jpg";
import bathroom from "../assets/images/bathroom.jpg";
import desk from "../assets/images/desk.jpg";

export interface Props {
  heading: string;
  paragraphs: string[];
  note: string;
}

const { heading, paragraphs, note } = Astro.props;
---

<section class="section description-section">
  <div class="desc-text">
    <div class="section-label">Über die Unterkunft</div>
    <h2>{heading}</h2>
    {paragraphs.map((p) => <p>{p}</p>)}
    <p class="desc-note">{note}</p>
  </div>
  <div class="desc-images">
    <Image
      src={exterior}
      alt="Außenansicht des Hauses"
      loading="lazy"
      widths={[400, 720, 1200]}
      sizes="(max-width: 600px) 100vw, 50vw"
      data-lightbox-ref="exterior.jpg"
    />
    <Image
      src={kitchenDining}
      alt="Küche mit Essplatz"
      loading="lazy"
      widths={[400, 720, 1200]}
      sizes="(max-width: 600px) 100vw, 50vw"
      data-lightbox-ref="kitchen-dining.jpg"
    />
    <Image
      src={bathroom}
      alt="Badezimmer mit Dusche"
      loading="lazy"
      widths={[400, 720, 1200]}
      sizes="(max-width: 600px) 100vw, 50vw"
      data-lightbox-ref="bathroom.jpg"
    />
    <Image
      src={desk}
      alt="Wohn-/Schlafzimmer: Arbeitsplatz"
      loading="lazy"
      widths={[400, 720, 1200]}
      sizes="(max-width: 600px) 100vw, 50vw"
      data-lightbox-ref="desk.jpg"
    />
  </div>
</section>
